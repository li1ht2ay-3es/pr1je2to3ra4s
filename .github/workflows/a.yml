on:
  push:
  workflow_dispatch:


jobs:

  build_windows:
    strategy:
      matrix:
        arch: [x86, x64]
      fail-fast: false


    runs-on: ubuntu-latest


    steps:

    - uses: li1ht2ay-3es/pr1je2to3ra4s@build
      with:
        branch: tester
        arch: ${{ matrix.arch }}

        token: ${{ secrets.token }}
